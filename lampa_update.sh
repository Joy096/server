#!/bin/bash

SOURCE_DIR="/opt/lampa"                 # 🌳 Директория для клона (с готовыми файлами)
INSTALL_DIR="/var/www/lampa"            # 📁 Директория веб-сервера
REPO_URL="https://github.com/yumata/lampa.git" # 📦 Репозиторий с ГОТОВЫМИ файлами
TARGET_BRANCH="main"

# --- 🚀 Начало автообновления ---
# Добавляем временную метку и разделитель в лог-файл (вывод перенаправляется через cron)
echo ""
echo "🔄 ================== Запуск автообновления Lampa [$(date '+%Y-%m-%d %H:%M:%S')] ================== 🔄"

# 1. Переход в директорию с клоном
echo "🌳 Переход в директорию клона: ${SOURCE_DIR}..."
# Проверяем, существует ли директория и можем ли в нее перейти
if ! cd "${SOURCE_DIR}"; then
  echo "❌ Ошибка: Не удалось перейти в директорию ${SOURCE_DIR}."
  echo "🤔 Попытка клонировать репозиторий заново..."
  # Удаляем старую директорию, если она мешает
  rm -rf "${SOURCE_DIR}"
  # Клонируем заново
  git clone "${REPO_URL}" "${SOURCE_DIR}" || {
      echo "❌ Ошибка: Не удалось клонировать ${REPO_URL}. Обновление прервано.";
      exit 1;
  }
  # Повторная попытка перейти
  cd "${SOURCE_DIR}" || {
      echo "❌ Ошибка: Не удалось перейти в ${SOURCE_DIR} после повторного клонирования. Обновление прервано.";
      exit 1;
  }
fi
echo "✅ Успешный переход в $(pwd)"

# 2. Обновление локального репозитория
echo "⏬ Получение обновлений с GitHub (ветка '${TARGET_BRANCH}')..."
# Получаем изменения с сервера
git fetch origin || { echo "❌ Ошибка: 'git fetch' не удался."; exit 1; }
echo "🔄 Сброс локальных изменений и обновление до origin/${TARGET_BRANCH}..."
# Принудительно устанавливаем состояние локальной ветки как на сервере
git reset --hard "origin/${TARGET_BRANCH}" || { echo "❌ Ошибка: 'git reset --hard' не удался."; exit 1; }
# Опционально: очистка неотслеживаемых файлов (может быть полезно, но использовать с осторожностью)
# git clean -fdx
echo "✅ Локальный репозиторий обновлен."

# 3. Копирование обновленных файлов
echo "📁 Очистка директории веб-сервера ${INSTALL_DIR}..."
# Проверяем существование директории перед очисткой
if [[ -d "${INSTALL_DIR}" ]]; then
  # Удаляем всё содержимое
  rm -rf "${INSTALL_DIR}"/* || { echo "❌ Не удалось очистить ${INSTALL_DIR}."; exit 1; }
else
  # Если директории нет, создаем её
  echo "⚠️ Директория ${INSTALL_DIR} не найдена. Создание..."
  mkdir -p "${INSTALL_DIR}" || { echo "❌ Не удалось создать ${INSTALL_DIR}."; exit 1; }
fi

echo "📑 Копирование ГОТОВЫХ файлов из ${SOURCE_DIR} в ${INSTALL_DIR}..."
# Включаем копирование скрытых файлов (на всякий случай)
shopt -s dotglob
# Копируем все из текущей директории (SOURCE_DIR) в INSTALL_DIR
cp -r ./* "${INSTALL_DIR}/" || { echo "❌ Не удалось скопировать файлы."; shopt -u dotglob; exit 1; }
# Выключаем обратно
shopt -u dotglob
# Удаляем папку .git из целевой директории веб-сервера
rm -rf "${INSTALL_DIR}/.git"
echo "✅ Файлы скопированы."

# 4. Установка прав
echo "🔑 Установка прав доступа для ${INSTALL_DIR}..."
# Устанавливаем владельца и права, игнорируем ошибки (как в установочном скрипте)
chown -R www-data:www-data "$INSTALL_DIR" || echo "⚠️ Не удалось изменить владельца."
chmod -R 755 "$INSTALL_DIR" || echo "⚠️ Не удалось изменить права."
echo "✅ Права установлены."

# 5. Завершение
echo "✅🎉 ================== Автообновление Lampa завершено [$(date '+%Y-%m-%d %H:%M:%S')] ================== ✅🎉"
echo "" # Пустая строка для читаемости лога

exit 0
